[project]
name = "thy-squeal-e2e"
version = "0.1.0"
description = "End-to-end test suite for the ThySqueal server"
requires-python = ">=3.10"

[dependency-groups]
dev = ["pytest>=8", "httpx>=0.27"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
